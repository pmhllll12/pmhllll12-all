# =============================================================================
# 생성된 산출물 (그래프 등)
# =============================================================================
minho/apps/titanic/_generated/

# =============================================================================
# 비밀값
# =============================================================================
.env
# pgAdmin이 pgvector 자동 로그인에 쓰는 접속 파일 — DB 비밀번호가 평문으로 들어간다.
# servers.json(PassFile 경로 지정)은 비밀값이 없으므로 계속 추적한다.
docker/pgadmin/pgpass

# =============================================================================
# 파이썬 가상환경
# =============================================================================
.venv/
